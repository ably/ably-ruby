name: Features

on:
  pull_request:
  push:
    branches:
      - main

permissions: {}

jobs:
  build:
    permissions:
      contents: read
      deployments: write
      id-token: write
    uses: ably/features/.github/workflows/sdk-features.yml@main
    with:
      repository-name: ably-ruby
    secrets: inherit
